// Import the mobileNav function from mobileNav.js
import mobileNav from './mobileNav.js';


// Initialize mobileNav when the DOM content is fully loaded
document.addEventListener('DOMContentLoaded', () => {
    mobileNav(); // Call the function to activate mobile navigation
});
